{
  "name": "linear-agent",
  "version": "0.1.0",
  "description": "Linear OAuth + AgentSessionEvent webhook bridge for OpenClaw, with registered agent tools that drive the Linear session lifecycle.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.33",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@linear/sdk": "^82.1.0"
  }
}
